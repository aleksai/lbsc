import UIKit

extension XXColor {
    static let ball = XXColor.systemOrange
    static let floor = XXColor.white

    static let barrel: [Barrel.Kind: XXColor] = [
        .normal: .systemBlue,
        .zone: .systemGreen
    ]
}
